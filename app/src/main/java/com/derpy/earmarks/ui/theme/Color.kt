package com.derpy.earmarks.ui.theme

import androidx.compose.ui.graphics.Color

// Punk Science Studios brand palette v1.0 (2026).
val HardcoreBlack = Color(0xFF0E0E10) // ink — text on paper, ground when reversed
val NovaWhite = Color(0xFFF5F1EA)     // paper — warm off-white
val BuildAcid = Color(0xFFD6FF3F)     // signal — build dots, [ ok ], highlights
val HalifaxCobalt = Color(0xFF3A6BFF) // accent — links / success only

// Reversed-on-ink working tones derived from the ground.
val InkSurface = Color(0xFF16161A)    // lifted panel on the ink ground
val InkSurfaceVariant = Color(0xFF1E1E22)
val InkOutline = Color(0xFF3A3A42)
val MutedPaper = Color(0xFFB8B4AC)    // de-emphasised paper for secondary copy
val SignalRed = Color(0xFFFF6B6B)     // destructive / error attention
