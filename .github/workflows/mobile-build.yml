name: Build Mobile APK

# CI runs on PRs for verification, and on manual dispatch for release builds.
# No push trigger — merges don't fire wasteful CI runs.
on:
  pull_request:
    branches: [main]
    paths:
      - 'earmarks-mobile/**'
      - '.github/workflows/mobile-build.yml'
  workflow_dispatch:

permissions:
  contents: write # needed to create the Release

defaults:
  run:
    working-directory: earmarks-mobile

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Run unit tests
        run: ./gradlew testDebugUnitTest --no-daemon --stacktrace

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: earmarks-debug-apk
          path: earmarks-mobile/app/build/outputs/apk/debug/app-debug.apk

      # Publish a Release with the APK — only on manual dispatch, not PR checks.
      # Tags are prefixed `mobile-` so CLI releases share the repo without collision.
      - name: Publish release
        if: github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="mobile-build-${{ github.run_number }}"
          gh release create "$TAG" \
            app/build/outputs/apk/debug/app-debug.apk \
            --title "Earmarks mobile build ${{ github.run_number }}" \
            --notes "Automated debug build from ${{ github.sha }}. Download \`app-debug.apk\` and sideload it (enable install from unknown sources)." \
            --prerelease

      # Deliver APK directly to Darryl on Telegram — tap to install.
      - name: Send to Telegram
        if: github.event_name == 'workflow_dispatch'
        run: |
          curl -sS -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
            -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -F document=@app/build/outputs/apk/debug/app-debug.apk \
            -F caption="🎵 Earmarks mobile build ${{ github.run_number }} — tap to install"

      # Purge old mobile releases to stay under the 0.5GB free-tier storage cap.
      # Keeps only the latest; `mobile-` prefix filter leaves CLI releases alone.
      - name: Purge old releases
        if: github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release list --limit 100 --json tagName --jq '[.[] | select(.tagName | startswith("mobile-"))][1:][].tagName' \
            | xargs -r -n1 gh release delete --cleanup-tag -y
